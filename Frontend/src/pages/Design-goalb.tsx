import { HomeContent } from "../components/HomeContent";

// /design5 "אטלייה" — mirrors the real homepage (the chosen main direction),
// shown alongside /design7 (photographic) as the two finalists.
export default () => (
  <main className="page-main">
    <HomeContent />
  </main>
);
